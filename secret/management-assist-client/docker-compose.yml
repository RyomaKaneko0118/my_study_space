version: '3'
services:
  app:
    build:
      context: .
    #   dockerfile: Dockerfile
    # command: /bin/bash -c 'yarn dev'
    tty: true
    stdin_open: true
    ports:
      - 3333:3333
    # networks:
    #   - seminar-assist-network
    volumes:
      - .:/management-assist-client
# networks:
#   seminar-assist-network:
#     external: true
