# management-assist-client

## Framework

- SvelteKit

## Requirements

- nodejs 18.15.0

## Setup

```
$ docker-compose build
$ docker-compose run --rm app yarn install

.envをコピーし適宜環境変数を設定してください
$ cp .env.example .env.development
```
